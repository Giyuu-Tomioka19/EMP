<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#F59301"/> <!-- Здесь укажите желаемый цвет фона -->
    <size
        android:width="80dp"
        android:height="80dp"/> <!-- Укажите размеры для создания круга -->
    <stroke
        android:width="2dp"
        android:color="#FFFFFF" /> <!-- Border color -->

    <!-- Optionally, you can add padding inside the border -->
    <padding
        android:left="6dp"
        android:top="6dp"
        android:right="6dp"
        android:bottom="6dp" />

    <!-- Optionally, you can round the corners -->
    <corners android:radius="25dp" />
</shape>